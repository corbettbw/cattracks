<% flash.each do |type, message| %>
  <div class="mb-4 px-4 py-2 rounded text-sm
    <%= type == 'alert' ? 'bg-red-100 text-red-800' : 'bg-green-100 text-green-800' %>">
    <%= message %>
  </div>
<% end %>